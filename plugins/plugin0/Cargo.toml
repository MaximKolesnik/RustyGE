[package]
name = "plugin0"
version = "0.1.0"
authors = ["lagobas <maxim.kolesnik90@gmail.com>"]

[dependencies]
ecs = { path = "../../engine/ecs" }
plugin_loader = { path = "../../engine/plugin_loader" }
reflection = { path = "../../engine/reflection" }

[lib]
crate-type = ["cdylib"]
